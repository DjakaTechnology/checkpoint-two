# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Checkpoint' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Checkpoint
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'SDWebImage/WebP'

  target 'CheckpointTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CheckpointUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
